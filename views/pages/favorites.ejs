<div class="title2">
   <h1>
      <%= user.username %>'s
      Favorite Jokes
   </h1><br>
</div>
<div>
   <% user.saved_jokes.forEach(saved_joke =>{ %>
      <div class="jumbotron">
         <h2><%= saved_joke.joke_content %></h2>
      </div>
   <% }) %>
</div>

